// Create Event modal JS
function handleSubmitEvent(event) {
    event.preventDefault();
    // Her ville du typisk sende data til backend
    window.location.href = 'dashboard.html';
}

// Hvis du har modal-funktioner, kan de tilføjes her
